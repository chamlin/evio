apply plugin: 'java'

repositories {
    maven {
        url 'http://developer.marklogic.com/maven2'
        mavenCentral()
    }
}

task dist() << {
    println 'building the distribution'
}

dist {
    description  = 'dist task'
}

dependencies {
    compile 'com.marklogic:java-client-api:3.0.5'
}

   // workingDir 'src/main/java'
   // commandLine 'pwd > /tmp/pwd.out'
   // commandLine 'javac -Djava.ext.dirs=/Users/chamlin/jars/java-client-api-3.0.5/ com/esereno/evio/*.java test.java'
